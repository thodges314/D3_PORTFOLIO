// Stub for EmbedLookBasic — the original component requires a Looker host environment.
// This no-op placeholder prevents import errors in ShowMoreDialog.
import React from 'react';

export const EmbedLookBasic = () => (
  <div style={{ padding: '20px', textAlign: 'center', color: '#999' }}>
    <p>Embedded Look view (requires Looker host environment)</p>
  </div>
);
